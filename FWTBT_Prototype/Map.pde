/*class Map

  Map(){}
  
  map.loadPixels();
  for(int y = 0; y<map.height;y++){
    for(int x = 0; x<map.width; x++)
    {
      int cPixel = y+x*map.height;
      if(map.pixels[cPixel] == color(0,0,0))
      {
        map.pixels[cPixel] = color(255,0,0);
      }
    }
  }
  map.updatePixels();   
  
}*/
